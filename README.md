# Enhanced Dynamic Memory Requirements Prediction for Scientific Workflow Tasks
## Getting Started

### Installation

`pipx install poetry`

`poetry install -e`

### Checkout the submodules

`git submodule update --init --recursive`

### Run main.py

`poetry shell`

`python approach/main.py`
